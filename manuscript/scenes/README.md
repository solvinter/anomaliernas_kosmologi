# Scenes

This folder contains worked scene drafts.

Each scene should trace back to one or more source fragments in `notes/fragments/`.

Use `production/scene_template.md` when creating a new scene.

Scene files should normally use the suffix:

```text
*.scene.md
```

Example:

```text
blackrocks_return.scene.md
```
